---
const theme = import.meta.env.THEME || 'default';
const siteName = import.meta.env.PUBLIC_SITE_NAME || 'default';

const { default: DocumentationPage } = await import(`../../websites/${siteName}/themes/${theme}/page/documentation.astro`);
---

<DocumentationPage />

