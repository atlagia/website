---
import { SITE_TITLE } from '../consts';
import { Book, Video, MessageCircle, LifeBuoy, Mail, MessageSquare, FileText, BookOpen, Calendar, ChevronDown } from 'lucide-react';

const theme = import.meta.env.THEME || 'default';
const lang = Astro.params.lang || 'en';

const { default: BaseHead } = await import(`../components/BaseHead.astro`);
const { default: Header } = await import(`../components/Header.astro`);
const { default: Footer } = await import(`../components/Footer.astro`);

// Import language-specific support data
const supportData = await import(`../data/support/support_${lang}.json`);

const businessInfo = {
  name: import.meta.env.PUBLIC_BUSINESS_NAME || 'Atalsia'
};

// Map icon strings to actual components
const iconMap = {
  Book,
  Video,
  MessageCircle,
  LifeBuoy,
  Mail,
  MessageSquare,
  FileText,
  BookOpen,
  Calendar
};

// Map categories with actual icon components
const categories = supportData.categories.map(category => ({
  ...category,
  icon: iconMap[category.icon]
}));

// Map contact options with actual icon components
const contactOptions = supportData.contact.options.map(option => ({
  ...option,
  icon: iconMap[option.icon]
}));

// Map resources with actual icon components
const resources = supportData.resources.items.map(item => ({
  ...item,
  icon: iconMap[item.icon]
}));
---

<!doctype html>
<html lang={lang}>
  <head>
    <BaseHead 
      title={`Support | ${businessInfo.name}`}
      description={supportData.hero.description}
    />
  </head>
  <body class="bg-[#04252B]">
    <Header />
    <main class="pt-10">
      <!-- Hero Section -->
      <section class="relative py-20 px-4 bg-gradient-to-b from-[#04252B] via-[#052F37] to-[#04252B] overflow-hidden">
        <div class="absolute inset-0">
          <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-gradient-to-bl from-emerald-500/10 to-cyan-500/10 rounded-full blur-3xl animate-blob animation-delay-4000"></div>
          <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-[500px] h-[500px] bg-gradient-to-tr from-emerald-500/10 to-cyan-500/10 rounded-full blur-3xl animate-blob"></div>
        </div>
        <div class="relative max-w-7xl mx-auto text-center">
          <span class="inline-block px-4 py-1.5 mb-6 text-sm font-semibold rounded-full bg-emerald-500/20 text-emerald-300 border border-emerald-500/30">{supportData.hero.subtitle}</span>
          <h1 class="text-4xl md:text-5xl font-bold mb-6 text-white">{supportData.hero.title}</h1>
          <p class="text-xl text-slate-300 max-w-2xl mx-auto">
            {supportData.hero.description}
          </p>
        </div>
      </section>

      <!-- Support Categories -->
      <section class="relative py-16 px-4 bg-[#052F37]">
        <div class="max-w-7xl mx-auto">
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
            {categories.map(category => (
              <a
                href={category.link}
                class="group p-8 bg-[#08343C]/70 rounded-2xl hover:bg-emerald-600 transition-all duration-300 border border-emerald-900 hover:border-emerald-500"
              >
                <div class="flex flex-col items-center text-center">
                  <div class="w-14 h-14 mb-6 rounded-xl bg-emerald-500/20 group-hover:bg-emerald-500/40 flex items-center justify-center transition-colors">
                    <category.icon className="w-7 h-7 text-emerald-400 group-hover:text-white" />
                  </div>
                  <h3 class="text-xl font-semibold mb-2 text-white group-hover:text-white">{category.title}</h3>
                  <p class="text-slate-300 group-hover:text-gray-200">{category.description}</p>
                </div>
              </a>
            ))}
          </div>
        </div>
      </section>

      <!-- FAQ Section -->
      <section class="relative py-16 px-4 bg-[#052F37]">
        <div class="max-w-4xl mx-auto">
          <div class="text-center mb-12">
            <h2 class="text-3xl font-bold mb-4 text-white">{supportData.faq.title}</h2>
            <p class="text-slate-300">{supportData.faq.description}</p>
          </div>
          <div class="space-y-8">
            {supportData.faq.categories.map(category => (
              <div class="bg-[#08343C]/70 rounded-2xl p-6 shadow-sm border border-emerald-900">
                <h3 class="text-xl font-bold mb-4 text-white">{category.title}</h3>
                <div class="space-y-4">
                  {category.questions.map(item => (
                    <details class="group">
                      <summary class="flex justify-between items-center cursor-pointer list-none">
                        <span class="font-medium text-white">{item.question}</span>
                        <ChevronDown className="w-5 h-5 text-emerald-400 group-open:rotate-180 transition-transform" />
                      </summary>
                      <p class="mt-4 text-slate-300">{item.answer}</p>
                    </details>
                  ))}
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      <!-- Contact Options -->
      <section class="relative py-16 px-4 bg-[#04252B]">
        <div class="max-w-7xl mx-auto">
          <div class="text-center mb-12">
            <h2 class="text-3xl font-bold mb-4 text-white">{supportData.contact.title}</h2>
            <p class="text-slate-300">{supportData.contact.description}</p>
          </div>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-4xl mx-auto">
            {contactOptions.map(option => (
              <a
                href={option.link}
                class="flex flex-col items-center p-8 bg-[#08343C]/70 rounded-2xl hover:bg-emerald-600 group transition-all duration-300 border border-emerald-900 hover:border-emerald-500"
              >
                <div class="w-14 h-14 mb-6 rounded-xl bg-emerald-500/20 group-hover:bg-emerald-500/40 flex items-center justify-center transition-colors">
                  <option.icon className="w-7 h-7 text-emerald-400 group-hover:text-white" />
                </div>
                <h3 class="text-xl font-semibold mb-2 text-white group-hover:text-white">{option.title}</h3>
                <p class="text-slate-300 group-hover:text-gray-200 mb-4 text-center">{option.description}</p>
                <span class="font-medium text-emerald-400 group-hover:text-white">{option.action}</span>
              </a>
            ))}
          </div>
        </div>
      </section>

      <!-- Additional Resources -->
      <section class="relative py-16 px-4 bg-[#052F37]">
        <div class="max-w-7xl mx-auto">
          <h2 class="text-3xl font-bold mb-12 text-center text-white">{supportData.resources.title}</h2>
          <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            {resources.map(resource => (
              <a
                href={resource.link}
                class="flex items-start p-6 bg-[#08343C]/70 rounded-xl hover:shadow-md transition-shadow border border-emerald-900 hover:border-emerald-500"
              >
                <div class="w-12 h-12 rounded-xl bg-emerald-500/20 flex items-center justify-center mr-4">
                  <resource.icon className="w-6 h-6 text-emerald-400" />
                </div>
                <div>
                  <h3 class="font-semibold mb-1 text-white">{resource.title}</h3>
                  <p class="text-sm text-slate-300">{resource.description}</p>
                </div>
              </a>
            ))}
          </div>
        </div>
      </section>
    </main>
    <Footer />
  </body>
</html>

<style>
  .bg-navy-900 {
    background-color: #0a0b1e;
  }
  .bg-navy-800 {
    background-color: #12132d;
  }

  @keyframes blob {
    0% {
      transform: translate(-50%, -50%) scale(1) rotate(0deg);
    }
    33% {
      transform: translate(-50%, -50%) scale(1.1) rotate(120deg);
    }
    66% {
      transform: translate(-50%, -50%) scale(0.9) rotate(240deg);
    }
    100% {
      transform: translate(-50%, -50%) scale(1) rotate(360deg);
    }
  }

  .animate-blob {
    animation: blob 20s infinite linear;
  }

  .animation-delay-4000 {
    animation-delay: 4s;
  }
</style> 