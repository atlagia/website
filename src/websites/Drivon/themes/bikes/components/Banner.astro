---
import { ArrowRight } from 'lucide-react';

interface Props {
  headline: string;
  description: string;
  backgroundImage?: string;
  button1Text: string;
  button1Link: string;
  button2Text?: string;
  button2Link?: string;
}

const { headline, description, backgroundImage, button1Text, button1Link, button2Text, button2Link } = Astro.props;
---

<section
  class="drivon-banner relative min-h-[70vh] flex items-center justify-center overflow-hidden"
  style={backgroundImage ? `background-image: url(${backgroundImage}); background-size: cover; background-position: center;` : undefined}
>
  {backgroundImage ? (
    <div class="absolute inset-0 bg-[var(--drivon-bg)]/85 z-10" aria-hidden="true"></div>
  ) : (
    <div class="absolute inset-0 drivon-banner-bg z-0" aria-hidden="true"></div>
  )}

  <div class="relative z-20 max-w-[840px] mx-auto px-6 sm:px-8 text-center py-24 lg:py-32">
    <p class="font-display text-[10px] tracking-[0.4em] uppercase text-[var(--drivon-accent)] mb-8">Racing heritage</p>
    <h2 class="font-display text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold text-[var(--drivon-text)] uppercase tracking-tighter leading-[1.02] mb-6">
      {headline}
    </h2>
    <p class="text-base md:text-lg text-[var(--drivon-text)]/70 max-w-xl mx-auto mb-12 font-body tracking-wide">
      {description}
    </p>
    <div class="flex flex-wrap justify-center gap-5">
      <a
        href={button1Link}
        class="group inline-flex items-center gap-3 bg-[var(--drivon-accent)] text-[var(--drivon-bg)] px-8 py-4 text-[11px] font-medium tracking-[0.25em] uppercase hover:bg-[var(--drivon-accent-hover)] transition-all duration-300"
      >
        <span>{button1Text}</span>
        <ArrowRight className="w-4 h-4 group-hover:translate-x-0.5 transition-transform" />
      </a>
      {button2Text && button2Link && (
        <a
          href={button2Link}
          class="group inline-flex items-center gap-3 border border-[var(--drivon-text)]/60 text-[var(--drivon-text)] px-8 py-4 text-[11px] font-medium tracking-[0.25em] uppercase hover:border-[var(--drivon-accent)] hover:text-[var(--drivon-accent)] transition-all duration-300"
        >
          <span>{button2Text}</span>
          <ArrowRight className="w-4 h-4 group-hover:translate-x-0.5 transition-transform" />
        </a>
      )}
    </div>
  </div>
</section>

<style>
  .font-display { font-family: var(--font-display); }
  .font-body { font-family: var(--font-body); }
  .drivon-banner-bg {
    background: linear-gradient(180deg, var(--drivon-bg) 0%, var(--drivon-surface) 50%, var(--drivon-bg) 100%);
  }
</style>
