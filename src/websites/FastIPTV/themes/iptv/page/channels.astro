---
import { getLangFromUrl, useTranslations } from '@utils/i18n';
const { default: BaseHead } = await import(`../components/BaseHead.astro`);
const { default: Header } = await import(`../components/Header.astro`);
const { default: Footer } = await import(`../components/Footer.astro`);

const lang = getLangFromUrl(Astro.url);
const translations = await useTranslations(lang);
const jsonData = translations.default;

// Load channels data dynamically
let channelsData;
try {
  const channelsModule = await import(`../data/channels/channels_${lang}.json`);
  channelsData = channelsModule.default;
} catch (error) {
  // Fallback to English if language-specific file doesn't exist
  const channelsModule = await import(`../data/channels/channels_en.json`);
  channelsData = channelsModule.default;
}

const SITE_TITLE = jsonData.siteTitle || 'FastIPTV';
const PAGE_TITLE = channelsData.pageTitle + ' | ' + SITE_TITLE;
const PAGE_DESCRIPTION = channelsData.pageDescription;
const categories = channelsData.categories;
const breadcrumbs = [
  { name: 'Home', url: `/${lang}` },
  { name: 'Channels', url: `/${lang}/channels` }
];
---

<!doctype html>
<html lang={lang} dir={lang === 'ar' ? 'rtl' : 'ltr'}>
  <head>
    <BaseHead title={PAGE_TITLE} description={PAGE_DESCRIPTION} breadcrumbs={breadcrumbs} />
  </head>
  <body class="bg-[#0a0a0a] text-white">
    <Header lang={lang} />
    <main class="pt-24"> <!-- Add 96px (24 * 4px) padding to account for header height + extra space -->
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <section class="space-y-12">
          {/* Hero Section */}
          <div class="text-center space-y-4">
            <h1 class="text-4xl md:text-6xl font-bold gradient-text">
              {channelsData.heroTitle}
            </h1>
            <p class="text-xl text-gray-300 max-w-3xl mx-auto">
              {channelsData.heroDescription}
            </p>
          </div>

          {/* Search and Filter */}
          <div class="glass rounded-xl p-6">
            <div class="flex flex-col md:flex-row gap-4">
              <div class="flex-1">
                <input
                  type="search"
                  placeholder={channelsData.searchPlaceholder}
                  class="w-full px-4 py-2 rounded-lg bg-white/10 border border-white/20 focus:border-purple-500 focus:ring-2 focus:ring-purple-500"
                />
              </div>
              <div class="flex gap-4">
                <select class="px-4 py-2 rounded-lg bg-white/10 border border-white/20 focus:border-purple-500 focus:ring-2 focus:ring-purple-500">
                  <option value="">{channelsData.allCategories}</option>
                  {categories.map(cat => (
                    <option value={cat.name.toLowerCase()}>{cat.name}</option>
                  ))}
                </select>
                <select class="px-4 py-2 rounded-lg bg-white/10 border border-white/20 focus:border-purple-500 focus:ring-2 focus:ring-purple-500">
                  <option value="">{channelsData.allQualities}</option>
                  <option value="4k">4K</option>
                  <option value="hd">HD</option>
                </select>
              </div>
            </div>
          </div>

          {/* Channel Categories */}
          <div class="space-y-12">
            {categories.map(category => (
              <div class="space-y-6">
                <div class="flex items-center gap-4">
                  <span class="text-3xl">{category.icon}</span>
                  <h2 class="text-2xl font-bold text-purple-400">{category.name}</h2>
                </div>
                <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-6">
                  {category.channels.map(channel => (
                    <div class="glass rounded-xl p-4 hover-scale hover-glow">
                      <div class="aspect-video bg-white/5 rounded-lg mb-4 flex items-center justify-center">
                        <div class="text-2xl font-bold">{channel.name}</div>
                      </div>
                      <div class="flex items-center justify-between">
                        <span class="text-sm text-gray-300">{channel.quality}</span>
                        <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-green-500/20 text-green-400">
                          {channelsData.liveLabel}
                        </span>
                      </div>
                    </div>
                  ))}
                </div>
              </div>
            ))}
          </div>

          {/* CTA Section */}
          <div class="glass rounded-2xl p-8 text-center space-y-6">
            <h2 class="text-3xl font-bold gradient-text">{channelsData.ctaTitle}</h2>
            <p class="text-xl text-gray-300">
              {channelsData.ctaDescription}
            </p>
            <div class="flex justify-center gap-4">
              <a
                href={channelsData.viewPlansUrl}
                class="bg-purple-600 hover:bg-purple-700 text-white font-bold py-3 px-8 rounded-lg transition duration-200"
              >
                {channelsData.viewPlansButton}
              </a>
              <a
                href={channelsData.startTrialUrl}
                class="bg-emerald-600 hover:bg-emerald-700 text-white font-bold py-3 px-8 rounded-lg transition duration-200"
              >
                {channelsData.startTrialButton}
              </a>
            </div>
          </div>
        </section>
      </div>
    </main>
    <Footer />
  </body>
</html> 