# repbag.shop - HTTP only (for certbot first run)
# After: sudo certbot --nginx -d repbag.shop -d www.repbag.shop
# Then replace this file content with the full SSL version and reload.

server {
    listen 80;
    server_name repbag.shop www.repbag.shop;

    location / {
        proxy_pass http://127.0.0.1:7005;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }
}
